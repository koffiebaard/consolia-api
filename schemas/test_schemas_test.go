package main

import (
    _"fmt"
    "testing"
    _"github.com/stretchr/testify/assert"
    _"text/tabwriter"
    _"os"
)

func TestValidateSchemaOfEndpoint(t *testing.T) {

    /*
    token := "ihqy1nguQm6IWM5P7rI59g"

    w := new(tabwriter.Writer)
    w.Init(os.Stdout, 0, 8, 0, '\t', 0)

    err := validateSchemaOfEndpoint(w, "comic.json", "GET", "/comic/get_upcoming_comic", token)
    assert.NoError(t, err)

    err = validateSchemaOfEndpoint(w, "comics.json", "GET", "/comic", token)
    assert.NoError(t, err)

    err = validateStatusCodeOfEndpoint(w, 200, "GET", "/", token)
    assert.NoError(t, err)

    err = validateStatusCodeOfEndpoint(w, 401, "GET", "/", "invalidTokenTest")
    assert.NoError(t, err)
    */
}
